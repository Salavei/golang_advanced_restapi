---

is_debug: true
listen:
  type: port
  bind_ip: 0.0.0.0
  port: 10000
storage:
  postgresql:
    host: localhost
    port: 5432
    database: user-service
    username: goroot
    password: go123456
    max_attempts: 5
  mongodb:
    host: localhost
    port: 27017
    database: user-service
    auth_db:
    username:
    password:
    collection: users